{
  "name": "cloudbrowse",
  "version": "1.0.0",
  "private": true,
  "description": "A virtual browser you use from your browser: a real Chromium runs on the server and is pixel-streamed to the client over WebSocket.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "puppeteer-core": "^23.11.1",
    "ws": "^8.18.0"
  },
  "engines": { "node": ">=20" }
}
